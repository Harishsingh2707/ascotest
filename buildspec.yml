version: 0.2

phases:
  pre_build:
    commands:
      - apt-get update -y
      - apt-get nodejs -y
      - apt install npm -y
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - '**/*'
cache:
  paths:
    - '/root/.m2/**/*'
    - '/root/.npm/**/*'
    - 'build/**/*'
